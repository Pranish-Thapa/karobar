services:
  - type: web
    name: karobar
    runtime: node
    buildCommand: cd client && npm install && npm run build && cp -r dist ../server/public
    startCommand: cd server && npm install && node src/index.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
